// Fill these in from your Supabase project:
// Project Settings → API → Project URL, and the "anon public" key
// (NOT the service_role / secret key — the anon key is meant to be public,
// it's safe to commit and deploy since Row Level Security protects your data).

const SUPABASE_URL = "PASTE_YOUR_SUPABASE_PROJECT_URL_HERE";
const SUPABASE_ANON_KEY = "PASTE_YOUR_SUPABASE_ANON_PUBLIC_KEY_HERE";
